using System;
using System.Collections.Generic;
using System.Globalization;
using System.Text;

namespace Secao6ComportamentoDeMemoriaArraysListas
{
    class VetoresPT2
    {
        //public static void Main(string[] args)
        //{
        //    int n = int.Parse(Console.ReadLine());

        //    //Cria objeto(vetor de um objeto)
        //    //Product[] vetor;

        //    //Instancia objeto(vetor de um objeto)
        //    //new Product[n];

        //    Product[] vect = new Product[n];

        //    //É PRECISO INSTANCAIR CADA UM DOS OBJETOS DENTRO DO VETOR JÁ INSTANCIADO
        //    //ESTA É A DIFERENCA DE UM VETO DO TIPO CLASSE EM RELACAO A UM VETOR DO TIPO STRUCT

        //    for (int i = 0; i < n; i++)
        //    {
        //        string name = Console.ReadLine();
        //        double price = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

        //        vect[i] = new Product { Name = name, Price = price };
        //    }

        //    double soma = 0.0;
            
        //    for (int i = 0; i < n; i++)
        //    {
        //        soma += vect[i].Price;
        //    }

        //    double avg = soma / n;
        //    Console.WriteLine("O preço médio = " + avg.ToString("F2",CultureInfo.InvariantCulture) );

        //}
    }
}
