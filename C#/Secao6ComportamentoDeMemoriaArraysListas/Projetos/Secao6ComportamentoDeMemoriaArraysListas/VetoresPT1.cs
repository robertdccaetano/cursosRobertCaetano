using System;
using System.Collections.Generic;
using System.Globalization;
using System.Text;

namespace Secao6ComportamentoDeMemoriaArraysListas
{
    class VetoresPT1
    {
        //public static void  Main(string[] args)
        //{
        //    //arrays sao arranjos unidimensionais
        //    //(homogeneo = mesmos tipos de dados, ordenado = acessado por meio de posicoes, alocacao = continua na memoria

        //    int n = int.Parse(Console.ReadLine());

        //    //Cria um vetor do tipo double e instancia o mesmo com 'n' elementos
        //    double[] vect = new double[n];

        //    for (int i = 0; i < n; i++)
        //    {
        //        vect[i] = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);
        //    }

        //    double sum = 0.0;

        //    for (int i = 0; i < n; i++)
        //    {
        //        sum += vect[i];
        //    }

        //    double avg = sum / n;
        //    Console.WriteLine("Average height = " + avg.ToString("F2", CultureInfo.InvariantCulture));

        //}
    }
}
